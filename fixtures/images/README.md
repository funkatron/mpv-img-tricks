# Image fixtures

Regenerate solid-color PNGs (for manual smoke tests) from the repo root:

```bash
./tests/manual/generate-fixtures.sh
```

See [tests/manual/README.md](../../tests/manual/README.md).
